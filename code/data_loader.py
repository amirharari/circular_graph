def load_data(connectivity_matrix, atlas_path, grouping_name, calculating_field_name):
    load_data(connectivity_matrix, atlas_path,
              grouping_name, calculating_field_name)

    return []
